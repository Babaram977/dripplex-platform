# @dripplex/merchant-portal

Dripplex merchant portal — live Backend Core integration via sdk-merchant.

Program C Phase C1 — Backend Core integration scaffold.
Public SDK barrel: `src/lib/sdk-merchant.ts` (re-exports `@dripplex/sdk/sdk-merchant`).
