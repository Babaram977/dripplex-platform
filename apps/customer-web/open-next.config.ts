import { defineCloudflareConfig } from '@opennextjs/cloudflare';

/**
 * OpenNext Cloudflare adapter config for customer-web.
 * Incremental R2 cache can be enabled later — see docs/ops/CLOUDFLARE-CUSTOMER-WEB.md
 */
export default defineCloudflareConfig({});
