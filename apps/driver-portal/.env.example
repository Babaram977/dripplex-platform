NEXT_PUBLIC_API_BASE_URL=http://localhost:3000/api/v1
NEXT_PUBLIC_APP_URL=http://localhost:3004
# Where referral shares link to — the customer-facing app, not this portal.
NEXT_PUBLIC_CUSTOMER_APP_URL=http://localhost:3001

# Google Maps (Launch Mode Phase 1) — same browser key as customer-web.
# UNRESTRICTED for now (dev unblock); must be restricted by HTTP referrer
# to production domains before launch — see docs/LAUNCH-READINESS-CREDENTIALS.md.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=AIzaSyB6NzMSnB5nPcTE3rhR_XihCokp-Dkk4-A
