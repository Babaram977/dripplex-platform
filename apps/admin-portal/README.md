# @dripplex/admin-portal

Dripplex admin portal — live Backend Core integration via sdk-admin.

Program C Phase C1 — Backend Core integration scaffold.
Public SDK barrel: `src/lib/sdk-admin.ts` (re-exports `@dripplex/sdk/sdk-admin`).
