{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "allowJs": true,
    "checkJs": true,
    "types": ["node"]
  },
  "include": ["eslint.config.js", "scripts/**/*.ts"],
  "exclude": ["node_modules", "apps", "packages", "dist", "build", ".next"]
}
