# `@dripplex/hooks`

Shared React hooks and portal providers.

## Exports

- `AppProviders` — TanStack Query + toast host
- `ThemeToggle` — light / dark / system theme control
- `useThemeStore` — persisted theme state (Zustand)
